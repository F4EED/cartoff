#!/bin/sh
# Incrémente version.json (sans hash de commit) et l'ajoute au commit.
cd "$(git rev-parse --show-toplevel)" || exit 1
. "$(dirname "$0")/run-python.sh"

run_cartoff_python scripts/bump_version.py --skip-commit-hash || exit 1
git add version.json
